export default function Register() {
    return <h1>Register</h1>
}

//if you want to ignore the auth or the routing group name in url path in bowser or rounting
// just put the routing group name in parenthesis () while nameing the routing group folder 
// eg. --> (auth)